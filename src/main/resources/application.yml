spring:
  jpa:
    database: POSTGRESQL
  datasource:
    url: ${POSTGRES_URL}
    username: ${POSTGRES_USER}
    password: ${POSTGRES_PASS}
rakutenapp:
  id: ${APP_ID}
appconfig:
  updateIntervalMinutes: 90
  poppingHours: 24
  expireHours: 100
  nPage: 20
  nMaxInsert: 150
