version: 2.1
jobs:
  build:
    docker:
      - image: cimg/openjdk:11.0
      - image: cimg/postgres:14.0
        environment:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: password
          POSTGRES_DB: testdb
    steps:
      - checkout
        - run:
          name: Waiting for Postgres to be ready
          command: dockerize -wait tcp://localhost:5432 -timeout 
      - run:
          name: Create table.
          command: |
            sudo apt install -y postgresql-client || true
            psql --host=localhost:5432 -f ./src/main/resource/db/table.sql
      - run:
          name: Build app.
          command: |
            source local_script.sh
            ./gradlew build
            